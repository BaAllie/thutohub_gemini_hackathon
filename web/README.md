# Web Configuration

This directory contains web-specific configuration files.

## Files to import:
- `index.html` - Main HTML file
- `manifest.json` - Web app manifest
- `favicon.png` - Favicon

## Note
Web support is optional but allows running the app in browsers.
